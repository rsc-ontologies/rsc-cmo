# Mappings

This repository contains semantic mappings
from CHMO to external vocabularies, encoded in SSSOM.

SSSOM files can be formatted with:

```console
$ uv tool install sssom-pydantic
$ sssom_pydantic format fix-mappings.sssom.tsv
```
